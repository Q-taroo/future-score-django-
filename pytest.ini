[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = tests.py test_*.py *_tests.py
# tests_e2e/ needs a live server + browser and is run separately
# (see tests_e2e/test_signup_and_vote.py's docstring) — excluded from
# the default fast unit/integration run.
testpaths = tests
